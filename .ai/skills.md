# Project skills

Each project skill lives in `.ai/skills/<name>/SKILL.md`. Read the matching skill before starting the task it covers.

## Available Skills

- **update-image-engine** (`.ai/skills/update-image-engine/`): bumping sharp, SVGO or the SSIMULACRA 2 WASM crate. It covers the checks each engine needs, running the golden tests, treating every changed decision as an API change, regenerating `tests/golden/golden.json`, and recording the change in `CHANGELOG.md`.
