#!/usr/bin/env node
/**
 * CLI entry for `wio` and `web-image-optimiser`, and the only module that reads
 * `process.env` or `process.argv`.
 */
process.stderr.write("wio: no commands are available yet\n");
process.exitCode = 2;
